<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('partials/head') %> 
</head>
<body>
    <header class="headerEdit">
        <img class="logoAdmin" src="/images/logo-principal.png" alt="logo" />
        <div class="userAdmin">
            <p>Usuario: Nombre Usuario</p>
            <p>Cerrar Sesión</p>
        </div>
    </header>
    <main class="main-carga-productos-panel">
        <h2 class="carga-productos-panel-adm">Editando producto <%= product.id  %> </h1>
            <h4 class="nuevo-producto-panel-adm">producto</h3>
                <form action="/admin/edit/<%= product.id %>?_method=PUT" method="POST" enctype="multipart/form-data">
                <div class="carga-form-productos" >
                    <div class="container-productos-carga-individual">
                        <input class="producto-individual-carga" type="text" placeholder="Modelo" name="name" value="<%= product.name %>" />
                        <input class="producto-individual-carga"type="text" placeholder="Marca" name="brand_id" value="<%= product.brand_id %>">
                        <input type="text" class="producto-individual-carga" placeholder="Descuento" name="discount" value="<%= product.discount %>">
                        <!--  -->
                        <input type="text" class="producto-individual-carga" placeholder="Precio" name="price" value="<%= product.price%>">
                    </div>
                    <p id="desc-producto-carga-productos">Descripcion del producto</p>
                    <textarea class="desc-product-text-area" name="description" id="descripcion" ><%= product.description %></textarea>
                    <p class="add-file-img-carga-products">Agregar archivos de imagen</p>
                    <div class="container-img-products-carga-end">
                        <input  class="buttonForm" type="file" id="file" name="product-image" multiple /><br><br>
                        <div id="submit-fn">
                            <button type="submit" id="btn-submit-finalizar-edit">Finalizar</button>
                        </div>
                    </div>                  
                </div>
            </form>
                </main>
            </body>
            </html>